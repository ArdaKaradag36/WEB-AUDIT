# Web Audit: PR = smoke (no strict); main = regression + quality gate (--strict).
# Exit codes: 0 ok, 1 crash, 2 quality gate fail (critical>0 or error>5 or warn>20).

name: Web Audit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  audit:
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: "runner/.nvmrc"
          cache: "npm"
          cache-dependency-path: runner/package-lock.json

      - name: Install runner deps
        run: npm ci
        working-directory: runner

      - name: Lint
        run: npm run lint
        working-directory: runner

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}
        working-directory: runner

      - name: Build
        run: npm run build
        working-directory: runner

      - name: Start local test server
        run: |
          node -e "
          const http = require('http');
          const server = http.createServer((req, res) => {
            res.writeHead(200, {'Content-Type': 'text/html'});
            res.end('<html><head><title>CI Smoke Site</title></head><body><h1>CI Smoke</h1><a href=\"/about\">About</a><form method=\"GET\" action=\"/search\"><input name=\"q\" type=\"text\"/><button type=\"submit\">Search</button></form></body></html>');
          });
          server.listen(9876, () => console.log('Smoke server on :9876'));
          " &
          sleep 2
        working-directory: runner

      - name: Run audit (smoke)
        run: node dist/cli.js --url http://localhost:9876 --max-links 5 --max-ui-attempts 15 --out reports/runs/${{ github.run_id }}-${{ matrix.browser }}
        working-directory: runner

      - name: Run audit (strict, main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          node dist/cli.js --url http://localhost:9876 --max-links 5 --strict --out reports/runs/${{ github.run_id }}-${{ matrix.browser }}-strict
        working-directory: runner

      - name: Upload audit artifacts
        uses: actions/upload-artifact@v4
        with:
          name: audit-${{ matrix.browser }}
          path: runner/reports/
          if-no-files-found: ignore

  runner-tests:
    name: Runner tests (Node 20)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: "runner/.nvmrc"
          cache: "npm"
          cache-dependency-path: runner/package-lock.json

      - name: Install runner deps
        run: npm ci
        working-directory: runner

      - name: Lint
        run: npm run lint
        working-directory: runner

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('runner/package-lock.json') }}

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium
        working-directory: runner

      - name: Build
        run: npm run build
        working-directory: runner

      - name: Run tests
        run: AUDIT_ALLOW_PRIVATE_TARGETS=true npm test
        working-directory: runner
